from django.apps import AppConfig


class CyclesConfig(AppConfig):
    """Configuration for the cycles app."""
    
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.cycles'
    verbose_name = 'Review Cycles'